# Slay the Spire GBA Demake — Progress

Goal: playable .gba rom, Slay the Spire act 1, Ironclad, direct clone names (personal use). Emulator: mGBA.
Repo: https://github.com/austinsomer/spire-gba (build/ untracked — `make` regenerates build/spire.gba)

## Status (2026-07-13)
- [x] Relic-view → map render artifacts FIXED (see completed entry below).
- [x] In-depth card inspect + inspect on purchase/reward (see completed entry below).
- [x] Battle card energy-cost visibility (see completed entry below).
- [x] Power cards: once per combat, leave play, return next combat (see completed entry below).
- [x] Pre-intro splash slides (see completed entry below).
- [x] Intro audio (see completed entry below). NOTE: user simplified — no fade; on start-press the intro music just STOPS, then the title track begins.
- [x] Opening credits + intro video — DONE. The "opening credit slides" ARE the pre-intro splash slides (MEGA CRIT / Austin Somer, see "Pre-intro splash slides" completed entry); intro video + intro audio also done (see their completed entries). Full cold-boot cinematic: bios → 2 credit/splash slides (2s each, fading) → intro video (with music) → flashing PRESS START → stop music + fade → title.
- [x] Map→room transition animation (see completed entry below). NOTE: sfx intentionally deferred — see the new "Transition SFX" to-do below.
- [ ] Transition SFX (future): add a short sound the length of the map→room fade (fx_out/fx_reveal in engine.c). User asked to defer audio. Could be per-destination (combat whoosh, shop chime, event shimmer) via the existing sfx layer — trigger at the start of fx_out.
- [x] Title text "NEW GAME" → "NEW RUN" (see completed entry below).
- [ ] Sound for various enemies dying (creature death sound for non bipedal enemies, a death cry for humanoid enemies). ON HOLD (2026-07-13): user will refine requirements + provide the audio. A first pass (synth dcry/dbeast + classification + trigger at the two hit_enemy/thorns kill sites) was built then fully REVERTED at user request — tree is clean. When resuming: the death happens where hp<=0 sets alive=0 (combat.c hit_enemy + the thorns branch; split/escape are NOT deaths); mksfx.py can drop-in real audio by putting files in assets/sfx/source/<slot>.<ext> (any ffmpeg format) — add the new slot(s) to its `slots` tuple + optional ALIASES, then wire SFXP_* enum (game.h) → sfxpcm_play cases (pcm.c) → sfx_* wrappers (engine.c). Humanoid set was CULTIST/LOOTER/SLAVER/NOB, everything else creature — user to confirm the split (constructs Sentry/Guardian, ghost Hexaghost, beast Lagavulin are the ambiguous ones).
- [x] Toolchain: arm-none-eabi-gcc 16.1.0 (brew), mGBA.app (brew cask)
- [x] Build scaffold: Makefile, src/gba.ld, src/crt0.s, tools/gbafix.py (logo+checksum). `make` → build/spire.gba, `make run` opens mGBA
- [x] Engine (src/engine.c, src/gba.h, src/game.h): mode 0, BG0 text layer (font8x8 → 4bpp, 10 color banks), BG1 UI layer (boxes, HP bar tiles, icons), vsync via vblank IRQ + swi 0x05, input w/ key repeat, xorshift rng, PSG sfx blips
- [x] Title screen boots in mGBA, VERIFIED via screenshot (60fps, renders correct)
- [x] Task 2: cards (src/cards.h/.c): 32 Ironclad cards + 4 statuses, upgrades, piles (draw/hand/discard/exhaust), deck browser UI. VERIFIED in mGBA.
- [x] Task 3: combat (src/combat.c): full act 1 roster + elites Nob/Lagavulin/Sentries + bosses SlimeBoss(split)/Guardian/Hexaghost, intents w/ live damage numbers, weak/vuln/str/thorns/metallicize/barricade/demonform, targeting UI, autoplay bot (-DAUTOPLAY) ran all 13 encounters clean: normals+Nob+Sentries won, Lagavulin+bosses killed starter deck (expected)
- [x] Task 4: map (src/map.c) 7x15 4-walk gen, room routing; screens (src/screens.c): reward (gold/card-of-3/elite relic), shop (5 cards/relic/remove), rest (heal/smith), 4 events (Big Fish, Golden Idol, Cleric, Living Wall), treasure, gameover, victory; 8 relics w/ combat hooks. stubs.c deleted.
- [x] Enemy sprites: 15 32x32 OBJ sprites (tools/art.txt ASCII → tools/mksprites.py → src/sprites.h). OAM shadow committed in vsync (OAM ignores writes outside vblank). VERIFIED in mGBA (slimes w/ faces render).
- [x] FIXED: memset self-recursion — gcc pattern-matched freestanding memset body into a memset call → infinite recursion, SP underflow, wild exec. mem.o builds w/ -fno-builtin -fno-tree-loop-distribute-patterns. Found via mGBA gdb stub (-g) + raw RSP python client (no arm-gdb on box; lldb batch dies on SIGINT). Single-step SP trace nailed it.
- [x] Makefile: header deps now tracked ($(HDRS)) — stale engine.o w/ old sprites.h cost a debug cycle.
- [x] Task 5: 6+ min monkey soak clean (deep runs, shop/relics cycling, sprites across species), ship build boots to title. GAME COMPLETE.
- [x] UI overhaul (user mockups): BG2 scenery layer (charblock 1, sbb 29, banks 10-15, tools/mkbg.py + tools/bgart.txt), scene composers (src/scene.c). Title: 2x gold logo (txt_put2x synthesizes scaled glyphs into cb0 200+), wall+torches, ironclad+cultist sprites. Map: icon nodes (skull/elite/fire/chest/coin/?/boss) + legend. Battle: stage layout — ironclad left (oam5), enemies right at 120+i*40, intents above, hp bars below, card-strip hand (5 slots, type-colored frames), L/R select. Shop: keeper sprite (looter), panel list, desc on bottom line. All VERIFIED via mGBA window screenshots.
- Art authored by subagent: 7 bg tiles + 7 icons (tools/bgart.txt), @ironclad sprite (art.txt).
- [x] Map overhaul: BG2 = 32x64 scrolling layer (SB 28+29, vofs camera). 16x16 sage-backfilled icons (mkbg @big: + !opaque), runtime dashed/solid path patterns + dim bank 14 + line/cursor bank 15 (src/map.c). UP/DOWN pans, camera centers current floor. MAPTEST define = pre-walked map for screenshots. Pattern tiles rebuilt on every map_screen entry (mode-4 title clobbers cb1).
- [x] Bitmap title: user key art via tools/mkimage.py (mode 4, 254-color quantize, font8x8 1.5x "NEW GAME" bake at y~120, arrow on reserved pal slot 255 → blinked by palette cycling in title loop). Source png kept at tools/title_src.png. Mode-4 exit MUST: video_init + sprites_load + bg2_load + txt2x_reset (bitmap clobbers charblocks 0-2; bg2_load re-blanks cb1 tile 0).

- [x] Battle UX overhaul (user mock): image backdrop via tools/mkbattlebg.py (deduped tiles at index 320+, bank 9 entries 4-14, battle_bg_load per combat entry), framed hp bars top, status lines w/ icons, intent icon+dmg, shadow sprites, 4-slot big card row (cost orb/name banner/16x16 art inset via BG2 window in BG1 fill/dmg). Sprites prio 2 (HUD renders over). FIXED: sprite palettes >16 banks wrapped into BG bank 0 (mksprites now packs shared banks); gcc strlen emission (mem.c). BATTLETEST define boots straight into jaw worm fight.
- [x] PNG asset pipeline: tools/assets.py export/import + ASSETS.md — user edits PNGs in assets/, importer splices back into art sources w/ palette-budget validation. Round-trip verified.

- [x] Full-card art (2026-07-12): user's 3 Gemini sheets (asset-grids/) sliced → assets/cards/*.png (36 cards, 40x56, alpha corners). tools/mkcards.py → src/cardimg.h (per-card 12-color k-means palettes, 4bpp 5x7 tiles, 40KB). Render: engine card_face_load/stamp — tiles streamed into cb1 gap 176-315 (4 slots x 35), palettes into TEXT banks 0-3 entries 4-15 (text uses only 1-3; OBJ banks were full: 14/16 used). combat draw_hand rewritten: 4 slots at cols 4/10/16/22 rows 13-19, yellow ui_box sel (interior cleared — ui_box fills!), live cost orb/dmg/blk overlays; STAGE_Y 80→64 so sprites+shadows end at card top (y104); enemy mini-bars row 13→12, enemy name row 12→1, hints → row 0 center. face_loaded[] cache reset at combat entry (cb1 reload). Dup variants on sheets resolved (defend/flex/pommel/carnage/entrench/flamebarrier); extra "STATUS" card on sheet A unused. VERIFIED: BATTLETEST screenshots + AUTOPLAY soak.

- [x] Battle screen redesign (2026-07-12, user's 3 Nano Banana sheets in assets/battle-ui/source_assets/):
  - Slicing: scratchpad script (slicegrid variant — per-cell tallest-content-band picks icon over its text label; relic sheet masked by chroma/brightness, junk bands skipped) → assets/battle-ui/{hud,relics}/*.png at final sizes. Relic sheet only had 6/8 cells — anchor + bloodvial hand-pixeled 16x16 in matching palette.
  - tools/mkhud.py → src/hudimg.h: HUD set (10 elems, 57 tiles) + relic set (8 icons, 32 tiles), each set one joint 12-color k-means palette. Data guarded by HUDIMG_DATA (engine.c defines it; combat.c includes for H_* enum only).
  - VRAM/palettes: hud+relic tiles stream to cb1 gap 63..151 (hud_load, called from battle_bg_load); bank 4 = HUD, bank 5 = relics, entries 4-15. Card faces now 5 slots (tiles 176..350, banks 0-3 + 6 via face_bank[]). mkbattlebg.py reworked: 240x80 cover-crop, 30x10 map, BATTLEBG_BASE 352 (273 uniq tiles), scene_battle stamps rows 2-11 only; rows 0-1 + 12-19 = tile 0 = near-black backdrop.
  - Layout (combat.c): top bar rows 0-1 = relic_stamp x nrelics + H_COIN at col 24 + gold; arena rows 2-11 w/ STAGE_Y 56, framed hp bars in-arena row 11 (player hp/max cols 2-7; enemies hp-only — 3 sentry bars would collide), 16x16 intent hud_stamp + dmg at rows 4-5 (boss 2-3), H_TARGET arrow above intent when targeting, enemy name right-aligned row 2, player status lines rows 2+; row 12 = card name + desc centered (targeting: A:ATTACK B:CANCEL); hand rows 13-19 = 5 slots cols 4/8/12/16/20 (8px overlap, stamped L→R, selected stamped last + raised to row 13, others row 14 bottom-clipped); hand HUD: H_ENERGY (0,13) + n/3 overlay, H_DRAWPILE (0,17) + count, H_ENDTURN (26,13) + R hint, H_DISCARDPILE (28,17) + count.
  - VERIFIED: BATTLETEST screenshot (layout + all elems), AUTOPLAY soak ~5 min (boss Hexaghost, 3-relic bar, sentries, shops, victory, map — clean 60fps), ship build boots to title.

- [x] Audio pass + settings (2026-07-12): sfx rewritten around ch1 hardware sweep (engine.c ch1()/ch4() helpers — sweep reg (time<<4)|(down<<3)|shift): ok = rising chirp, bad = falling womp, blip = 25%-duty tick, heal = slow shimmer up, block = dull tone + 7-bit metallic ch4 clank, hit = low sweep-down thump + noise crunch (layered ch1+ch4), new sfx_coin = sweep-up ding till overflow cutoff (reward gold, treasure, shop buys). Settings: opt_music/opt_sfx globals (engine.c, session-only); every sfx_* gates on opt_sfx; music_enable() silences ch2/3 but keeps song row → resumes mid-song; music_tick gates on opt_music. Title: mkimage.py now bakes N menu lines each w/ own reserved blinkable arrow slot (TITLE_ARROW_PALIDX + line, TITLE_NLINES) — "NEW GAME"/"SETTINGS", UP/DOWN + blink-selected in title loop; SETTINGS → tiled-mode settings_screen() (screens.c, wall backdrop, MUSIC/SOUND FX/BACK, A or L/R toggles), returns and reloads bitmap. SETTINGSTEST define boots straight into it. VERIFIED: title menu + blink (two-phase captures), settings screenshot, AUTOPLAY soak passes menus into runs, ship build.

- [x] PCM music streaming (2026-07-12): researched GBA-period audio (Pokémon MP2K/Sappy: 5-voice 8-bit software mixer at 13379 Hz over DirectSound + PSG in parallel; Golden Sun: Camelot's 16-bit-mixing Sappy variant, SC-88/JV-1080 samples). Went with MP2K's transport minus the mixer: src/pcm.c streams one looping signed-8-bit mono track from ROM to FIFO A — timer 0 at 13379 Hz (reload 65536-1254; vblank-locked = exactly 224 samples/frame), DMA1 special/repeat mode (0xB640) refills with zero CPU. pcm_tick() (vsync) advances pos by 224, restarts DMA at loop point inside vblank. Loop seam pre-baked by tools/mkpcm.py (ffmpeg decode → trim silence → normalize → 0.6s tail-into-head crossfade → pad to 224 mult) from user's two local reference tracks (Downloads) → assets/music/{title,run}.pcm (~1.7MB each, ROM now 3.5MB / 32MB) + src/pcmdata.s (.incbin). Routing in main loop: ST_TITLE → PCM_TITLE, else PCM_RUN (pcm_play same-track = no-op). music_enable() → pcm_gate() pauses (DMA off) / resumes at kept pos. SOUNDCNT_H = 0x0306 (PSG 100% + DSA 100% L+R timer0). PSG tracker (sound.c) kept but idle — song_sts no longer played. PSG sfx layer over the stream in hardware. VERIFIED: AUTOPLAY soak 60fps deep into runs (boss fights) w/ stream active; ship build 3584KB boots. Audio quality = user to judge by ear (DSA volume: drop bit 2 of SOUNDCNT_H for 50% if too loud vs sfx).

- [x] Flicker fixes (2026-07-12, user-reported map L/R flicker): cause = cursor move broke to the outer loop → compose_map() = bg2_clear (2048 cells) + full repaint + banner every move. Now L/R restore the old cursor cell (sage_cell() helper, same mottle hash) and continue the input loop — map never recomposes on nav (matches the UP/DOWN pan path). blink=16 forces the cursor visible immediately at the new node. Same disease in combat: scene_battle's bg2_clear ran on every draw_all (card nav). Split: scene_battle() = full clear (combat entry only); scene_battle_refresh() = restamp arena rows 2-11 + blank hand rows 13-19 only (top-bar cells rows 0-1 are only ever relic/coin stamps redrawn identically; row 12 never gets BG2 stamps). VERIFIED: AUTOPLAY soak — map cursor no trails, combat/hand/boss clean, 60fps.

- [x] SRAM saves (2026-07-12): src/save.c — SRAM 0x0E000000 byte-wise only (8-bit bus), REG_WAITCNT SRAM=8cyc, "SRAM_V113" marker kept in ROM via volatile read (mGBA save-type autodetect → build/spire.sav). Layout: settings block @0 ('S''1' music sfx csum16), run block @16 ('R''1' len16 csum16 + payload = Run struct bytes + map blob (map_export/import, MAP_BLOB_SIZE 225) + rng state; total 385B of 32KB). Save on room-resolved→ST_MAP transition (main loop prev-state hook); save_settings on every settings toggle; save_run_clear at gameover/victory entry. Title rebaked 3 lines (NEW GAME/CONTINUE/SETTINGS, TITLE_ARROW_PALIDX 253+li) — CONTINUE skipped in cycle when no valid save; loads run+map+rng and jumps to map. save_init at boot before first pcm_play (persisted opt_music applies). VERIFIED: .sav hexdump (S1 block reflects soak-toggled music-off; R1 len 0x181=385, hp/gold/floor match), relaunch w/ save → monkey resumed at floor-15 Hexaghost w/ saved gold 260 within 12s.

- [x] Combat juice (2026-07-12): hit event queue (hitq[12] in combat.c — hurt_player/hit_enemy push {tgt,-1=player,dmg} on hp loss only, blocked hits stay silent) consumed by anim_hits() right after play_card and enemy_turn: 14 frames of decaying ±2px sprite shake (nudge_sprite re-places via obj_show/_big; dead targets skip shake, keep number), floating damage numbers on BG0 drifting rows 8→7→6 w/ color steps WHITE→ORANGE→GRAY (multiple hits stack columns +3), backdrop palette flash for hits ≥10 (scene.c battle_flash: bank 9 entries +9 clamped, 2 frames). JUICETEST define stretches anim 14→90 frames for screenshot verification. VERIFIED: JUICETEST burst capture caught floating orange dmg number over jaw worm mid-drift; normal-speed AUTOPLAY soak clean 60fps; ship build boots.

- [x] Sampled SFX (2026-07-12): FIFO B one-shots alongside the FIFO A music stream — timer1 @13379Hz, DMA2 special/repeat 0xB640, SOUNDCNT_H now 0x730E (PSG + DS A timer0 + DS B 100% L+R timer1). tools/mksfx.py synthesizes all samples from scratch w/ numpy (hit = 95→45Hz sine thump + noise burst; clang = 4 inharmonic partials + click; coin = 1320Hz ding + delayed 1980Hz; slash = swept 1-pole LP noise), s8 @13379Hz, each +448 zero samples so the frame-counted stop (sfxb_left in pcm_tick, DMA2 off at end) lands in silence; assets/sfx/*.pcm via src/sfxdata.s .incbin. Routing: sfx_hit alternates HIT/SLASH samples, sfx_block=CLANG, sfx_coin=COIN; blip/ok/bad/heal stay PSG ch1 (unused ch4 helper deleted). VERIFIED: AUTOPLAY soak 60fps both FIFOs live, ship build clean.

- [x] Card-play pop (2026-07-12): anim_card_play in combat.c — before play_card resolves (affordability prechecked so failed plays don't animate), the selected card restamps one row higher (row 12; scene_battle_refresh hand-zone blank extended to rows 12-19 to catch it) w/ its vacated bottom row cleared and the cost orb flashing WHITE/YELLOW for 10 frames, then hits land via anim_hits. VERIFIED: burst capture caught card raised a full row over the hand w/ white orb flash; AUTOPLAY soak 60fps; ship build clean.
- [x] Potions verify/finalize (2026-07-12): confirmed the full potion pipeline works end-to-end — POT_* single slot (run.potion), reward drop as menu act 4, shop row, potion_roll (30% normal/50% elite, empty-slot only), use_potion() effects (HEAL+12/STR+2/BLK+12/NRG+2), SELECT drink + AUTOPLAY auto-drink, HUD chip via potion_tags at col 18. No code changes needed (all correct). VERIFIED: AUTOPLAY soak ~2min held 60fps through multiple combats/victory/reward with an "ENERGY POT" reward drop offered (drop+menu path). Deterministic HUD/collision proof via a temp BATTLETEST+POTIONTEST build (reverted) forcing potion=NRG + all 8 relics: top bar showed 8 relic icons cols 0-15, green "NRG+" chip + "SEL" hint at col 18, coin+gold at col 24/27 — no overlap even worst-case. Ship build boots to title.
- [x] Map reachable-node markers (2026-07-12): reachable next nodes now show a steady cream dot marker (DOT_TILE = CURSOR_TILE+1, bank 15: color2 cream box / color1 dark outline / color3 sage backfill, built in gen_map_tiles) so every choice reads at a glance, not just the blinking cursor. Stamped once per compose at (NPX(c)-16, NPY(floor)-4)>>3 (same cell as cursor, left of icon) for each reachable c; selected node's cell then blinks CURSOR_TILE arrow / DOT_TILE in the input loop; L/R restore old-sel cell to DOT_TILE — all INCREMENTAL (no compose_map/bg2_clear in input loop, so no flicker/trails). Prior agent's scaffold was already complete+correct; verified rather than rewritten. VERIFIED via MAPTEST: single-reachable floor shows one dot left of the "?"; temp MAPMULTI build (reverted) forcing 3 reachable next-nodes showed 3 dots (one per reachable node, none on other-floor/unreachable icons); blink captures confirm selected shows cursor arrow while others hold steady dots; 60fps, no trails. Ship build boots to title.
- [x] Reward + shop big card faces (2026-07-13): the reward 3-card pick and the shop card list now render the SAME 40x56 BG2 card-face art used in the battle hand (engine card_face_load/stamp, banks 0-3+6), replacing the old text boxes — pure visual upgrade, no new assets. New screens.c helpers: slen/txt_center; card_frame() (border-only frame using T_EDGE_*/T_CORN_* — needed because ui_box FILLS its interior with an opaque T_PANEL that would cover the face); face_overlay() (live cost orb T_ENERGY + cost + dmg/blk badge on BG0/BG1 over the face, mirroring combat stamp_card). card_choice(): 3 faces at cols 3/12/21 (RCARD_X) rows 3-9, selected framed CLR_YELLOW / others CLR_GRAY, name centered under each (selected white), selected desc row 13, hint row 17; LEFT/RIGHT added to nav alongside UP/DOWN; scene_none() stamps faces once before the loop (BG2 map persists; header() only clears BG0/BG1) and again on return to wipe faces so the reward menu isn't drawn over them. shop_screen(): 5 faces at cols 1/7/13/19/25 (SCARD_X) rows 2-8 (5 slots exactly match 5 face banks), price centered under each row 9, selected framed yellow; relic/potion/remove/leave moved to text rows 11-14 on a ui_fill(0,10,30,5) panel, selected card name+desc rows 16-17, keeper sprite kept. Old big panel fill (rows 2-11, BG1, would have hidden faces) removed. Sold card: bg2_fill blanks the face + "SOLD" text. Remove-card path calls scene_shop() (bg2_clear) then re-stamps the unsold faces from the still-resident cb1 tiles (deck_browse only touches BG0/BG1+bg2 map, not the face tiles). VERIFIED: temp REWARDTEST (card_choice hook in reward_screen) + SHOPTEST (main.c) builds (both reverted) screenshotted — reward showed Whirlwind/Anger/Body Slam faces w/ yellow sel frame, cost orbs, dmg/blk, names, desc; shop showed 5 faces w/ prices 81/84/124/102/78, sel frame, relic/potion/remove/leave panel, name+desc, keeper. AUTOPLAY soak ~5min clean 60fps — deck grew to 12 (POMMEL STRK + SHOCKWAVE added via reward pick + shop buys, exercising both new paths). Ship `make` (3584KB) boots to title 61fps.
- [x] Combat pile inspect + big-card zoom (2026-07-13): (A) in-combat you can now VIEW the draw/discard/exhaust piles (StS parity — before, only HUD counts showed). deck_browse() was refactored: the scrolling-list core moved into `int pile_browse(const CardInst *arr, int n, const char *title, int pick_mode)` (cards.c) and `deck_browse()` is now a one-line wrapper `return pile_browse(run.deck, run.ndeck, title, pick_mode)` — so all 6 existing pick-mode callers (rest smith, shop remove, event purge, pause DECK, map/combat DECK) are byte-for-byte unchanged. Combat input loop (combat.c) added: L → DRAW PILE, UP → DISCARD, DOWN → EXHAUST (each guarded on count>0 so an empty pile is a silent no-op, no blackout), each mirroring the existing KEY_B/deck_browse recovery (obj_hide_all; scene_none; pile_browse; reset face_loaded[]; break → draw_all recomposes). On-screen hints in draw_player: 'L' by the bottom-left draw count, "UP" by the bottom-right discard count, and an "EX <n> / DN" mini-count in the left gutter (rows 15-16) for exhaust. Free keys chosen because R=end-turn, SELECT=potion, A/LEFT/RIGHT=hand, B=deck, START=pause were all taken; L/UP/DOWN were the only ones free. (B) big-card ZOOM in every view-mode list: pick_mode==0 lists now show "A:ZOOM" (pick_mode==1 keep "A:PICK", untouched) — A calls card_zoom() (cards.c), rendering the highlighted card as the 40x56 battle face (card_face_load/stamp slot 0, cols 12-16 rows 3-9) with a border-only frame (zoom_frame, green if upgraded else yellow — ui_box would fill over the face), live cost orb + dmg/blk badge (upgrade/X aware via card_cost/dmg/block), and centered name (green if +)/type/full desc; ANY KEY returns. Zoom uses BG2 + cb1 face tiles + palette bank 0 entries 4-15 (list text uses entries 1-3 only → no clobber, empirically confirmed); on return bg2_clear wipes the face and the list redraws (same black backdrop scene_none set). Combat resets face_loaded[] after any pile view since zoom reloads slot-0 face tiles/pal. No new assets; ROM still 3584KB. VERIFIED: temp PILETEST (pre-populated discard/exhaust + main.c combat boot) + ZOOMTEST (auto-open zoom) builds (both reverted) screenshotted — DISCARD list (4 cards, A:ZOOM B:BACK) and STRIKE zoom (face, frame, cost orb, dmg 8/block 5, name, type, desc) rendered clean 60fps. AUTOPLAY soak ~2min held 59-60fps — monkey randomly opened the in-combat DECK browser and pressed A, exercising the LIVE zoom (DEFEND/BASH zooms captured incl. desc line) and the zoom→list return (11-card deck list intact, no palette corruption); deck grew via reward/shop picks confirming the pile_browse refactor didn't break pick-mode callers. (Note: purple "1:" at top-right in AUTOPLAY shots is the pre-existing AUTOPLAY-only gstate/spinner debug HUD in key_poll, engine.c:157-158 — absent from ship builds.) Ship `make` (3584KB) boots to title 60fps.
- [x] In-run pause menu (2026-07-13): START opens a pause overlay in combat and on the map — RESUME / DECK / SETTINGS / SAVE & QUIT. pause_menu() (screens.c) darkens the backdrop (scene_none → BG2 black) and draws its OWN centered layout (not the shared menu() — that left-crammed text under full-width gray bars): a 2x "PAUSED" banner centered with even padding in a yellow ui_box (rows 4-7), a single framed menu panel (ui_box cols 7-22 rows 10-15) where ONLY the selected row gets a CLR_BLUE accent bar (ui_fill) with white centered label while the others sit gray on the dark T_PANEL interior, labels via txt_center(15,...), and an "A:SELECT  B:RESUME" help line row 17. sel persists across DECK/SETTINGS visits (returns to the item you were on). RESUME/B closes (returns 0), DECK → deck_browse("DECK",0), SETTINGS → reuses the existing settings_screen() (returns cleanly, no title-bitmap reload inside it — that's the title loop's job), SAVE & QUIT → save_run() then gstate=ST_TITLE (returns 1). Hooks: combat.c input loop KEY_START branch mirrors the KEY_B/deck_browse path — obj_hide_all(); pause_menu(); return on quit else break (outer loop's draw_all → scene_battle_refresh recomposes, same recovery KEY_B relies on); map.c input loop KEY_START — obj_hide_all(); pause_menu(); on quit bg2_scroll(0)+return else break to recompose. Decl int pause_menu(void) in game.h. SAVE&QUIT-mid-combat decision: the save snapshots the RUN (run.hp already reflects damage; run.floor was advanced past the node before combat began), NOT live combat state — so CONTINUE resumes on the map at the next node choices, the current fight abandoned with no reward and no replay (no save corruption, no reward duplication). VERIFIED: temp PAUSETEST build (main.c, reverted) screenshotted the overlay — 2x PAUSED banner, RESUME/DECK/SETTINGS/SAVE & QUIT list, cursor on RESUME, darkened backdrop, 60fps. AUTOPLAY soak ~90s clean 60fps (bot doesn't press START but the added branches compile into the hot combat/map loops without breaking them — deck browser reached 11 cards). Ship `make` (3584KB) boots to title.

- [x] Card upgrade preview (2026-07-13): the SMITH/upgrade picker now shows a BEFORE>AFTER preview of the highlighted card so you can choose which to upgrade. Extended pile_browse's pick_mode from {0 view/zoom, 1 pick} to add {2 pick + upgrade preview}: mode 2 returns the index exactly like mode 1 (A:PICK), but replaces the row-18 desc line with an upgrade_line() showing each stat that upgrading would change as label + before(white) ">" after(CLR_GREEN). Helpers in cards.c: pv_stat(x,y,lbl,a,b) renders one stat's before>after (green upgraded value) and advances x via txt_int_at, skipping unchanged stats; upgrade_line(y,c) copies the CardInst with .up=1 and diffs card_dmg/block/cost/draw/spval (labels DMG/BLK/COST/DRAW/EFF), printing "ALREADY UPGRADED" for maxed (c.up) cards and "NO STAT CHANGE" if nothing differs — never a bogus arrow. Call sites bumped 1→2: rest SMITH (screens.c "UPGRADE WHICH?") and Living Wall event GROW branch (m==1); remove/purge callers (shop REMOVE, Cleric PURGE, Living Wall FORGET) stay at 1 so they keep showing the plain desc, and all view/zoom (mode 0) + combat pile-inspect callers are untouched. cards.h pile_browse/deck_browse comments updated for mode 2. Fits the 30-col grid on row 18 (worst real case ~2-3 stats, e.g. "DMG8>10 EFF2>3") with no collision with the row-19 "A:PICK B:BACK" hint. No new assets; ROM still 3584KB. VERIFIED: temp SMITHTEST build (main.c, reverted) booting deck_browse("UPGRADE WHICH?",2) screenshotted twice — a pre-upgraded STRIKE+ selected showed gray "ALREADY UPGRADED"; a BASH selected showed "DMG8>10 EFF2>3" with the upgraded values in green, before values white, gray '>', no overflow/collision, 60fps. AUTOPLAY soak ~2.5min held 59-60fps through combats/reward/shop (deck grew to 11 via POMMEL STRK) with no crash from the mode change; the bot's mode-0 DECK views still showed the desc line + "A:ZOOM B:BACK" confirming non-upgrade modes unchanged. Ship `make` (3584KB) boots to title 60fps.

- [x] Relic tooltips (2026-07-13): you can now READ what each owned relic does mid-run — before, relics only showed as icons in the combat top bar (and by name in shop/reward) with no effect text anywhere. New `const char relic_desc[N_RELICS][24]` beside relic_names in screens.c (extern in game.h), one ≤23-char line per relic matched to the actual code hooks: BURNING BLOOD "HEAL 6 HP AFTER COMBAT", VAJRA "START COMBAT: +1 STR", BRONZE SCALES "START COMBAT: +3 THORNS", ANCHOR "TURN 1: +10 BLOCK", LANTERN "TURN 1: +1 ENERGY", STRAWBERRY "+7 MAX HP", BAG OF PREP "TURN 1: DRAW 2 CARDS", BLOOD VIAL "START COMBAT: HEAL 2 HP". New relic_view() (screens.c, proto in game.h): dark scene_none backdrop, "RELICS" title, "B:BACK" hint; one 2-row entry per owned relic = its 16x16 icon (BG2 via relic_stamp, cols 0-1) + name (white, col 3) + desc (gray, col 3). ICONS render outside combat because relic_view calls hud_load() first (streams the relic tiles/pal into cb1 bank 5 — they're otherwise only loaded during battle_bg_load, so the map/pause path had them absent); verified they draw. Empty run shows a centered "NO RELICS YET" box. Scrolls with UP/DOWN (VIS=8 = rows 2-17, "UP"/"DN" markers) when >8 owned (array caps 16; Act 1 has ≤8 so scroll rarely hit). B or A returns. Reachable via the PAUSE MENU (START in both combat and map): pause_menu opts[] grew 4→5 {RESUME, DECK, RELICS, SETTINGS, SAVE & QUIT}, box grown one row taller upward (ui_box 7,9,16,7 = rows 9-15), items IY 11→10 (rows 10-14), banner rows 4-7 + help row 17 unchanged, sel bound <3→<4, A-dispatch inserts sel==2 → obj_hide_all()+relic_view()+break (mirrors DECK's recovery) and shifts SETTINGS to sel==3. No new assets; ROM still 3584KB. Uses ICONS (not text-only) — hud_load outside combat worked cleanly. VERIFIED: temp RELICTEST (+RELICPAUSE variant) build in main.c (reverted) screenshotted both — relic list showed BURNING BLOOD/VAJRA/ANCHOR/BAG OF PREP each with correct icon + name + desc, 60fps; resized pause menu showed all 5 items centered in the framed box (RESUME highlighted, RELICS as new 3rd row), banner + help clean, 60fps. AUTOPLAY soak ~100s held 59.7-60fps through combats/map (new array + pause-menu resize compile into the hot loops with no regression). Ship `make` (3584KB) boots to title 59.7fps.

- [x] Card reward SKIP (2026-07-13): confirmed already implemented — card_choice() (screens.c) returns -1 on KEY_B ("A:TAKE B:SKIP" hint), reward_screen case 2 does `if(id>=0) deck_add(id); got_card=1;` so skip consumes the card reward without adding. No work needed.
- [x] Potion belt — 3 slots (2026-07-13): replaced the single `u8 potion` slot with a `u8 potions[3]` belt (StS parity, `N_POTION_SLOTS 3` in game.h). potion_first_empty() helper (screens.c); potion_roll gates on any-empty-slot; reward drop + shop buy land in first empty slot (shop `can` + buy both use potion_first_empty()). Combat HUD (combat.c draw_all): per-slot 2-char chips via new `potion_short[N_POTIONS][3]={"","HP","ST","BK","NG"}` at cols 18/20/22 (fits before coin col 24), "SEL" hint row 1 if any held. SELECT now opens potion_picker() (deck_browse-style recovery: obj_hide_all+scene_none, own vertical menu of held potions w/ blue/gray panel bars, UP/DOWN + A:DRINK/B:CANCEL, resets face_loaded on return) instead of tap-drinking; empty belt = silent no-op. use_potion(slot) takes a slot index, clears that slot, effects unchanged (HEAL+12 clamped/STR+2/BLK+12/NRG+2). Autoplay drinks first held slot. save.c is sizeof(Run)-based so the +2 bytes absorb automatically (old .sav run-blocks now fail len/csum → CONTINUE hidden, accepted). VERIFIED: temp BATTLETEST w/ belt=[HEAL,STR,ENERGY] screenshot showed "HP ST NG" green chips + SEL + coin/gold no collision 61fps; temp PICKTEST screenshot showed the picker (BLOOD POT./STR POTION/ENERGY POT, cursor+blue bar, A:DRINK B:CANCEL); AUTOPLAY soak 40s clean 60.5fps deck grew to 11 (belt logic in hot combat loop stable). Both temp hooks reverted. Ship `make` (3584KB) boots.

- [x] Neow's Blessing run-start event (2026-07-13): NEW GAME now opens a blessing screen before the first map (StS run-opener). neow_screen() (screens.c, proto in game.h) wired into title_screen() sel==0 between run_new() and gstate=ST_MAP. scene_none backdrop, 2x cyan "NEOWS BLESSING" title (txt_put2x, 28 cols) + "A WHALE OFFERS A GIFT" flavor + menu() of 4: MAX HP +8 (maxhp+8 & hp+8), GAIN 100 GOLD, REMOVE A CARD (deck_browse pick 1 → deck_remove; cancel re-shows menu), OBTAIN A CARD (reward_elite=0; card_choice() → deck_add; skip re-shows menu). Loop commits exactly one blessing (opts 1-2 always; 3-4 only if sub-picker not cancelled) then returns. Reuses existing static card_choice/menu/deck_remove/deck_browse — no new assets. menu() called allow_back=0 so it only returns 0..n-1 (never -1) → the else/*m==3*/ dispatch is safe. Save: Neow runs pre-first-ST_MAP with prev==ST_TITLE so the main-loop save hook skips it (blessing persists, saved on next map re-entry). VERIFIED: temp NEOWTEST hook (added→removed, tree clean) screenshot showed the 2x title + flavor + 4-option menu (MAX HP +8 selected, cursor+blue bar) 61fps. Ship `make` (3584KB) boots.

- [x] Enemy depth composition (2026-07-13): enemy sprites moved up + right so the arena reads with depth — player is the near anchor (bottom-left, unchanged x24/STAGE_Y56), enemies now sit further back on the receding floor plane. combat.c: `EN_X` base 128→138 (+10px right) and new `EN_SP` spacing macro = 40px normally / 34px when `nen>=3` (keeps the 3rd sentry from clipping the 240px right edge — verified fully on-screen). New `EN_Y = STAGE_Y-6` lifts normal enemies 6px (feet y88→y82, higher on the lit floor oval = farther); shadow tracks (`EN_Y+8`) so it stays grounded, and the framed hp bar drops row 11→10 for normals (`hpr = is_boss?11:10`) so it hugs the raised feet with no float. Bosses (2x, `is_boss`) get the rightward shift only — vertical kept at STAGE_Y so the 64px sprite/its intent don't collide the top relic/gold HUD (rows 0-1); their hp bar stays row 11. nudge_sprite (hit-shake) updated to EN_Y for normals so shake replaces at the new position. Intent icons (rows 2-5) + status (row 6) left as-is; the 6px lift keeps a clean gap above normal sprites. No new assets; ROM still 3584KB. VERIFIED via temp BATTLETEST captures (forced encounters, reverted): single Jaw Worm — diagonal depth reads, hp bar tracks feet at row 10; 3 Sentries (enc 9) — all three fully on-screen, hp bars 42/40/41 no collision/clip; Guardian boss (enc 11) — 2x sprite right-shifted, fully on-screen, intent clear of top HUD, hp bar row 11, 60fps. Ship `make` (3584KB) builds clean, boots to title.

- [x] Audio pass 2 (2026-07-13): (a) run/gameplay music replaced with "The City" (`mkpcm.py --run-only` keeps title.pcm; title music unchanged). (b) menu navigation beep removed — `sfx_blip()` is now a no-op (select/cancel feedback kept). (c) battle SFX made drop-in replaceable: `mksfx.py` imports `assets/sfx/source/<slot>.*` (any ffmpeg format → 8-bit/13379Hz one-shot, ~0.6s cap) with synth fallback per slot; slots hit/slash/clang(block)/coin plus new sampled heal + card-play (SFXP_HEAL/SFXP_CARD, sfx_card() on card resolve; heal moved off PSG). Aliases incl. block/gold/restore/play/card-play. Raw ripped source audio gitignored (personal-use), only derived *.pcm tracked. (d) **FIFO-underrun buzz fixes** (root cause: a sound stream ends but its timer keeps clocking the now-empty FIFO → it replays the last byte forever): music-off in settings left DSA/timer0 running → static (pcm_gate/pcm_stop now stop TM0 + flush FIFO A); an isolated SFX (reward COIN, first battle) left DSB/timer1 running → persistent buzz (pcm_tick now stops TM1 + flushes FIFO B when a one-shot ends; masked in combat only because rapid successive SFX kept restarting the FIFO). VERIFIED: temp MUSICDBG probe (added→removed) showed `R * M` (run track / DMA armed / music-on) on both MAPTEST and the real title→Neow→combat path — run music streams correctly (earlier AUTOPLAY "off" reading was the monkey toggling settings). AUTOPLAY soak ~60fps with all 6 imported samples, no regression. Ship `make` (4832KB) boots.
- [x] Map generation balance rules (2026-07-13, src/map.c): the 4-random-walk map now runs a post-assignment `balance_map()` pass that enforces StS-style pacing (the walks build the same node/edge DAG as before — only room TYPES are constrained, so traversability/dead-end-free is untouched). Edge/parent helpers on the mnode grid: `node_exists`, `has_up_edge(f,c,nc)`, `is_parent(f,c,pc)` (a floor f-1 col that has an up-edge into (f,c)), `parent_has(f,c,room)`, `triple_run(f,c)` (same-type run of 3 ending here). `node_bad(f,c)` = the rerollable rule set: back-to-back shop (shop parent), adjacent elite (elite parent), elite before floor 5, rest before floor 5, and no non-MONSTER type 3x along any path. Enforcement (matching the TODO's validate+reroll plan): (1) lock floors 0 & 1 to MONSTER (StS opener) — floor 8 treasure / floor 14 rest were already forced in roll_room; (2) up to 80 passes rerolling each bad node via roll_room, preferring a fresh valid roll else falling back to MONSTER (MONSTER is exempt from every rule → the pass always terminates); (3) a final ascending hard-pass forcing any residual violator to MONSTER (ascending so a demotion can't leave a child violating); (4) per-act caps MAX_ELITES 2 / MAX_SHOPS 3, demoting the excess (bottom-up, earliest survive) to MONSTER. Special case fixed: floor 14 is a *locked* forced rest, so a 12-13-14 rest run isn't caught by triple_run (node 14 is never rerolled) — added a rule flagging a floor-13 REST that has a REST parent, breaking it one step earlier while still allowing a lone rest near the top. Rest-before-boss is guaranteed structurally: every path converges through the all-REST floor 14. No save format change (map_export/import still raw room+edge bytes; old .sav loads whatever it stored). No new assets; ROM still 3584KB. VERIFIED: native harness (scratchpad, slices the REAL roll_room+balance_map+map_generate out of src/map.c so there's no copy drift) ran 20,000 seeds / 957k nodes → **0 rule violations** after the fix (floor0/1 all-monster, no btb shop, no adjacent/early elite, no early rest, no type-3x incl. the rest-into-14 case, floor14 always rest, caps ≤2 elites/≤3 shops). Distribution healthy: elites cluster at 2 (88% of runs), shops at 3 (74%), with natural variance down to 0. mGBA MAPTEST screenshot — floor-8 treasure row + mixed skulls/shop/events/elites, no clumping, 61fps. AUTOPLAY soak confirms balanced maps stay fully traversable to the boss. Ship `make` (3584KB) boots to title.

- [x] Relic-view → map render artifacts FIXED (2026-07-13, src/map.c): mid-run, opening the pause menu's RELICS view then returning to the map showed black-block garbage where the node connectors should be (user report: "black artifacts, missing connectors"). ROOT CAUSE: `relic_view()` calls `hud_load()`, which streams HUD+relic icon tiles into charblock-1 tiles **63..151** (HUD_TILE0=63 +57, RELIC_TILE0=120 +32). The map's connector/pattern tiles live in the SAME range (`PT_BASE=N_BGTILES=63` .. 63+4*36=207), so hud_load overwrites them. The pause KEY_START branch breaks to the map's outer loop → `compose_map()`, but that only rebuilds pattern tiles when `map_tiles_ready==0`; the flag was still 1 (set 0 only at `map_screen` entry), so the clobbered tiles were never regenerated until the NEXT `map_screen` entry — exactly why "moving to the next event resolved it." Node icons (tiles 1..62) were untouched, so only connectors corrupted. FIX: set `map_tiles_ready = 0` in the map input loop's KEY_START branch after `pause_menu()` returns non-quit, so the recompose rebuilds the pattern tiles (1 line). Combat pause is unaffected (battle keeps HUD tiles resident + recomposes faces on return). Scope: map only. No new assets; ship ROM 4832KB. VERIFIED: temp RELICBUG/RELICFIX MAPTEST builds (added→removed, tree clean) captured the exact bug and its fix side-by-side — RELICBUG (clobber, no rebuild) reproduced the black-block connectors on floor 7; RELICBUG+RELICFIX (clobber + `map_tiles_ready=0`+recompose) rendered clean dashed connectors on the identical map/seed. AUTOPLAY soak ~45s clean 59.7fps deep into a run (card-zoom open), no regression. Ship `make` (4832KB) boots.

- [x] Title text "NEW GAME" → "NEW RUN" (2026-07-13): the title menu labels are baked mode-4 text patches, not runtime strings — `tools/mkimage.py <src.png> <out.h> [menu line]...` renders each menu line into a blittable 1.5x-font patch in `src/title_img.h` (the C driver reflows/centers the visible patches at runtime). Regenerated from the kept `tools/title_src.png`: `python3 tools/mkimage.py tools/title_src.png src/title_img.h "NEW RUN" "CONTINUE" "SETTINGS"` — patch 0 width dropped 98→86px (shorter text), TITLE_NLINES/CONTINUE-hidden logic unchanged. No code change, no new assets. VERIFIED: ship `make` (4832KB) title screenshot shows "NEW RUN" (yellow, selected) above "SETTINGS", correctly centered, CONTINUE hidden (no save), 59.7fps. (Capture note: `screencapture` returns "could not create image from display" when the Mac display has slept — `caffeinate -u -t 3` wakes it first; for a work session run `caffeinate -d -t <sec> &` to block display sleep.)

- [x] Battle card energy-cost visibility (2026-07-13, src/combat.c + engine.c): the in-hand cost badge was a 1bpp flame glyph (T_ENERGY) with the digit stamped into the SAME 8x8 cell → flame + number collided and both sat over the card art = barely legible (user: "difficult to see how much energy a card consumes"). Fix: (1) new `T_ORB` tile (154) = opaque filled disc (`orb[8]={60,126,255,255,255,255,126,60}`, loaded via load_tile_rows fg=1 so corners stay transparent). (2) `stamp_card` now draws the orb (CLR_YELLOW when affordable / CLR_DKRED when not — same `can` test as before: cost<=energy && not SP_UNPLAYABLE, COST_X always affordable) with the digit on top in a contrasting color (CLR_DKRED on yellow / CLR_WHITE on dkred), so the number reads over any art; upgrade '+' unchanged. (3) (3) anim_card_play's play-flash orb switched T_ENERGY→T_ORB for consistency. [REMOVED 2026-07-13 per user: an earlier row-12 col-0 persistent cost readout for the selected card — with name/desc centered it sat orphaned next to the HP bar and read as a stray artifact; the per-card badges already show cost, so it was redundant. Reverted, keeping only the per-card orb badges.] No new assets; ship ROM 4832KB. VERIFIED: BATTLETEST screenshot (jaw worm) — every hand card shows a bright yellow orb w/ dark cost digit top-left; zoomed crop confirms selected STRIKE reads "1" on both the card badge and the row-12 col-0 readout, energy "3/3" beside it. AUTOPLAY soak ~40s clean 60fps deep in a run (live card-zoom on DEFEND, no corruption from the new tile/palette). Ship `make` (4832KB) boots. INVESTIGATED (no action needed): the card-face PNGs have a baked DECORATIVE flame orb (~12x12) in the top-left corner — no number, just art. My 8x8 live orb draws over its center and shows the correct cost digit; the baked flame frames it (reads as a proper energy orb), so nothing to clean. A "wrong 2 on STRIKE" I first suspected was actually the NEIGHBORING card's live orb (BASH, cost 2) peeking at the 8px card-overlap seam — correct data, StS-authentic fanned-card tuck, not a bug. COLORS INVERTED (2026-07-14, per user): the affordable/unaffordable orb+digit color pair was swapped so the cost NUMBER is WHITE when affordable and dark-RED when out of energy (was red/white). To keep both states readable I swapped the ORB too (a digit-only swap would put same-color text on same-color orb): affordable = CLR_DKRED orb + CLR_WHITE digit, unaffordable = CLR_YELLOW orb + CLR_DKRED digit. VERIFIED BATTLETEST crop: white numbers on dark-red orbs read clearly. Ship ROM 9600KB.

- [x] In-depth card inspect + inspect on purchase/reward (2026-07-13, src/cards.c + screens.c): the big-card zoom used to show only the static 22-char `desc` string; now it renders a COMPUTED effect breakdown (StS-style) so you see exactly what a card does. New `card_detail(CardInst, char[4][32])` in cards.c (with tiny local d_ap/d_ai string builders — no libc): line 1 = damage incl. multi-hit total ("DEAL 5 X2 (10)" for Twin Strike), Body Slam ("DEAL DMG = YOUR BLOCK") and Whirlwind ("DEAL 5 TO ALL, X TIMES") special-cased, TGT_ALL adds " ALL"; then GAIN n BLOCK; DRAW n; a status/special line covering every SP_* (VULN/WEAK/UPPERCUT/SHOCKWAVE weak+vuln, STR/FLEX/DEMONFORM strength, METALLICIZE end-turn block, FLAMEBARRIER thorns, BARRICADE, ENTRENCH, HEAVYBLADE str-xN, BLOODLET, DISARM, ANGER copy, HEADBUTT, FEED on-kill maxhp, IMMOLATE burn, OFFERING, BURN, UNPLAYABLE); then a flags line (ETHEREAL / EXHAUST). Up to 4 lines, each <30 cols, centered under the card; card_zoom now shows name(row12)/type(row13)/detail(rows15-18)/hint(row19) instead of the one desc line. Exposed `void card_inspect(int id,int up)` (cards.h) wrapping card_zoom for callers that only have ids. Inspect is now reachable everywhere you evaluate a card: DECK/combat pile browsers already had A:ZOOM (mode 0, now shows the breakdown); SHOP added SEL:INFO (shop_screen KEY_SELECT on a card → card_inspect → scene_shop + keeper + reload/stamp the 5 faces, mirroring the remove-card recompose so zoom's BG2 clear + slot-0 tile reuse are undone); REWARD card_choice added SEL:INFO (KEY_SELECT → card_inspect → scene_none + reload/stamp the 3 faces). Hints updated ("A:BUY  SEL:INFO  B:LEAVE", "A:TAKE  SEL:INFO  B:SKIP"). Note: totals are BASE values (zoom is combat-agnostic; live strength not added). UPDATE (2026-07-14): card_detail now leads with an energy-cost line for ALL cards — "COST N ENERGY" ("COST X ENERGY" for X-cost, "COST 0 ENERGY" for free), using card_cost(c) (upgrade-aware). Fits the 4-line dl[4] buffer (cost + ≤3 effect lines; guards prevent overflow). VERIFIED via DETAILTEST: Twin Strike "COST 1 ENERGY", Whirlwind "COST X ENERGY", Offering "COST 0 ENERGY" + 3 effect lines no overflow. No new assets; ship ROM 4832KB. VERIFIED: temp DETAILTEST builds (added→removed, tree clean) screenshotted TWIN STRIKE "DEAL 5 X2 (10)", BASH "DEAL 8 / APPLY 2 VULNERABLE", OFFERING "LOSE 6HP +2E DRAW 3 / EXHAUST" — all correct, 60fps. AUTOPLAY soak ~95s clean 59.7-60fps: the monkey opened the live zoom (ANGER "DEAL 6 / ADD A COPY TO DISCARD") and returned to the 11-card DECK list with no palette/tile corruption (A:ZOOM intact), confirming zoom enter/exit + the recompose paths are safe in the hot loops. Ship `make` (4832KB) boots.

- [x] Power cards — once per combat, leave play, return next combat (2026-07-13, src/combat.c + cards.c): BUG — play_card ended with `cd->exhaust ? pile_exhaust_card : pile_discard_card`, and the 4 power cards (Inflame/Metallicize/Barricade/Demon Form, CT_POWER, exhaust=0) fell to the ELSE → went to the DISCARD pile, got reshuffled into draw, and could be drawn+played AGAIN in the same combat (stacking their buff repeatedly). StS powers instead leave play the moment they resolve (the effect becomes an ongoing buff) and come back next combat. FIX: new `pile_remove_hand(int i)` in cards.c (just `hand_remove` — no discard, no exhaust), and play_card now routes `cd->type == CT_POWER` to it (attacks/skills unchanged: exhaust→exhaust pile, else→discard). Because powers are only pulled out of the per-combat `piles` (never out of `run.deck`), and combat_screen's `piles_init()` rebuilds the piles from `run.deck` each fight, the power returns automatically next combat — no deck bookkeeping needed. The buff itself is applied earlier in play_card's switch (pl.str/metallicize/barricade/demonform), so removing the card keeps the effect. No new assets; ship ROM 4832KB. VERIFIED: deterministic temp POWERTEST (BATTLETEST, no AUTOPLAY; seeds Inflame into hand, plays it in code, reverted) — combat HUD debug read "NO LEAK · STR 2 · GONE": the power is absent from discard+exhaust (NO LEAK), gone from hand (GONE), and its +2 strength persists (hand card damage rendered 8/10/8/8 = base+2, "STRENGTH+2" floater), so it cannot be redrawn/replayed this combat; return-next-combat is guaranteed by construction (run.deck untouched → piles_init re-adds it). Plain AUTOPLAY soak ~80s clean 60fps through multiple combats (deck grew via reward/shop), no regression in the play_card path. Ship `make` (4832KB) boots.
- [x] New 64x64 hero sprite swap (2026-07-14, tools/mkhero.py + src/heroimg.h + src/engine.c + src/game.h + src/combat.c): replaced the old 32x32 SPR_IRONCLAD with the armored warrior from `updated-protagonist-sprite.png` (2048x2048 source). Source has baked light-gray checkerboard bg that the prior magenta-flood approach kept; switched mkhero.py to a global neutral-light removal (R==G==B AND max>=205) which keys the bg without holing the character (the warrior's lights are warm tans/steels — only the bg reads as neutral gray). Char then cropped, LANCZOS-resized to fit CANVAS-2=62px wide × CHAR_H=52px tall, pasted feet-bottom with 1px BOTTOM_PAD → 11px top-pad (top empty, bottom row empty = feet at canvas y=62, screen y=88 — exactly the same feet position the old 32x32 sprite had at STAGE_Y+32=88). 1-bit alpha threshold (>=110 → opaque) cuts any resize fringing. Saturation bump 1.35x before PIL.quantize keeps the red cape vivid through 15-color quantize. EMITS: src/heroimg.h = 512 u32 tiles (1D row-major 4bpp, 8x8 grid) + hero_pal[15]. WIRING (engine.c): `#include "heroimg.h"`, HERO_TILE=512 (OBJ tile base past the 32x32 sprite table), HERO_BANK=14 (free OBJ palette bank — 0-13 used by other sprites); sprites_load now copies hero_tiles to OBJ VRAM at 0x06010000+HERO_TILE*16 and writes hero_pal into bank 14 entries 1-15; new `obj_show_hero(oam_i, x, y)` sets ATTR0_SQUARE | ATTR1_SIZE(3) (64x64) | ATTR2_TILE(HERO_TILE) | ATTR2_PAL(HERO_BANK) | ATTR2_PRIO(2). game.h: declare obj_show_hero. combat.c: draw_player and nudge_sprite (player branch) now call obj_show_hero(OAM_PLAYER, 8, 26) / obj_show_hero(OAM_PLAYER, 8+dx, 26) — OAM x=8 + 32px left pad = char center at screen x=40, matches the old 32x32 sprite's center exactly so the new larger hero occupies the same horizontal axis as the old one. The OAM_SHADOW0 at (24, 64) is still drawn first (z-order: shadow under hero), but the larger 64x64 hero canvas covers it almost entirely — the oval at y=80-90 (visible part of the shadow sprite) peeks out at the feet. No new assets beyond the one heroimg.h; ship ROM 9600KB (unchanged). VERIFIED: BATTLETEST screenshot (jaw worm) — the armored warrior reads clearly at the on-screen size, crisp pixel art at the 1.5x scale the user picked (vs the too-big 2x preview), red cape vivid, sword + helm + fur pauldrons all distinct; positioned where the old ironclad was (feet at y=88 above the HP bar). AUTOPLAY soak ~90s clean 60.2fps through multiple combats + card-zoom opens, no crash/garbage from the new tiles/palette bank. Ship `make` (9600KB) boots.

## Design decisions
- Player: Ironclad 80 HP, 3 energy, draw 5. Statuses: Wound/Burn/Slimed/Dazed.
- Screens run own loops, set `gstate` to advance (see main.c state machine).
- Verify visually: `make run`, then capture mGBA WINDOW directly (works even occluded): pyobjc Quartz CGWindowListCopyWindowInfo → find owner 'mGBA' w/ name → `screencapture -x -l<windowid>`. NOTE: mGBA auto-pauses when fully occluded — identical frames ≠ game hang. osascript keystrokes BLOCKED (no accessibility perm) → use `-DAUTOPLAY` builds: combat bot + monkey input in key_poll.
- `make EXTRA=-DAUTOPLAY` = self-playing build for soak tests. Ship build: plain `make` (do `make clean` when switching!).
- Text grid 30x20, txt_* on BG0, ui_* on BG1. Colors = palette banks (CLR_* enum).
- No libc: src/mem.c provides memcpy/memmove/memset (gcc emits calls for struct copies).
- Map: only current-floor nodes selectable, edges from 4 random walks (union at shared nodes, no dead ends).
- [x] Map→room transition animation (2026-07-13, engine.c + gba.h + map.c + combat.c + screens.c): Pokémon-style "zoom into the encounter" — hardware fade-to-black + BG mosaic, no art assets, no sfx yet (deferred per user). New regs in gba.h (REG_MOSAIC/BLDCNT/BLDALPHA/BLDY, BG_MOSAIC bit). engine.c `fx_out()` ramps BLDY 0→16 (brightness-DOWN, mode 3<<6, all layers incl. OBJ+backdrop as 1st target = whole screen darkens) + BG mosaic 0→6 over 10 frames then holds full black, and sets `fx_pending`; `fx_reveal()` reverses it over 10 frames and clears BLDCNT/MOSAIC + the BG mosaic bits — gated on `fx_pending` so it only fires after a real faded exit (screens reached otherwise, e.g. combat→reward, don't fade). Because BLDY+mosaic act on every layer, one routine works from ANY screen — content-agnostic. Wiring: map.c KEY_A node-select calls `fx_out()` (before bg2_scroll(0) so it fades the correctly-scrolled view, then snaps camera under black) for both normal + boss routes; the destination composes under black and reveals via `fx_reveal()` placed at each first-composed-frame: menu() (covers rest + all 4 events, drawn once per outer loop), treasure_screen (before its input loop), shop_screen (after the first full compose, before the input loop — no-op on later iterations since pending is cleared), combat_screen (after the first draw_all). No new assets; ship ROM 4832KB. VERIFIED: temp FXTEST (freeze mid-fade in fx_out + a map.c trigger, both reverted) MAPTEST screenshot showed the map mid-transition — clearly mosaic-blocked AND darkened together (the intended effect). Plain AUTOPLAY soak: the monkey ran many map→combat transitions, every destination revealed at FULL brightness (deck browser + card-zoom captures show no BLDY/mosaic residue → fx_reveal restores state cleanly), 59.7-60fps, no crash, no stuck-black screen. Ship `make` (4832KB) boots.

- [x] Intro video (2026-07-13, tools/mkvideo.py + src/intro.c + introdata.s/introimg.h + engine.c + main.c + Makefile): cold-boot mode-4 video that animates into the title art, plays before the title, skippable via A/START. Source `~/Downloads/sts-intro.mp4` (1280x720/24fps/10s). tools/mkvideo.py: ffmpeg → 15fps frames, cover-crop to 240x160 (matches the mkimage title crop), keeps only the first 116 frames — the source's own baked "PRESS START" fades in ~frame 119, so trimming at 116 ends on the fully-assembled title scene with NO baked text (onset found by measuring bottom-center brightness per frame). Each frame → adaptive 256-color quantize → assets/intro/intro.bin (per frame: 256×u16 BGR555 palette + 240x160 byte indices, 38912 B/frame, 116 frames ≈ 4.3MB); the LAST (held) frame is quantized to 254 colors with pal[254]=cream-white / pal[255]=black reserved for our own "PRESS START" text, baked in the title font (font8x8 @1.5x, black outline — same machinery as mkimage's menu patches) and emitted as intro_ps_patch in src/introimg.h. src/intro.c: mode 4 double-buffered (two pages at 0x06000000 / +0xA000), copies each frame to the hidden page (32-bit — VRAM isn't 8-bit writable), sets palette + flips page at vblank, 4 vblanks/frame = 15fps; A/START skips to the hold. Hold: page 0 = clean scene, page 1 = scene + PRESS START; flashing is just a page-flip every ~24 frames (no redraw). On A/START → sfx_ok + fade_to_black. Fades: new plain BLDY-only fades in engine.c (fade_to_black / fade_from_black, no mosaic — the "basic fade" the user asked for, distinct from the encounter fx_out/fx_reveal); fade_to_black sets a pending flag, and title_screen (main.c) calls fade_from_black() right after it composes the mode-4 title bitmap → seamless fade-up into NEW RUN/SETTINGS (title art ≈ the intro's final frame, so no visible jump). Intro only runs on the normal cold boot (the #else in main's boot switch; MAPTEST/BATTLETEST/SETTINGSTEST skip it). Makefile: introdata.o added to OBJ + a rule (incbin assets/intro/intro.bin). No audio yet (separate to-do: "The Elite Slain"). ROM 4832KB → 9248KB. VERIFIED in mGBA: mid-video frame renders (warrior+spire, 60fps); the held frame shows the full title scene with OUR flashing PRESS START (two captures 0.4s apart = text on then off, source's baked text absent); temp INTROAUTO build (auto-advance, reverted) captured the post-fade title = NEW RUN/SETTINGS at full brightness (clean fade-up, no BLDY residue); AUTOPLAY (monkey's real A/START presses) advanced intro→title→Neow→into a Spike Slime combat at 60fps, no crash — the whole chain works with real input. Ship `make` (9248KB) boots.

- [x] Pre-intro splash slides (2026-07-13, tools/mkslides.py + src/intro.c + slidesdata.s/slidesimg.h + engine.c + main.c + Makefile): two full-screen mode-4 splash cards shown after the GBA bios, before the intro video — "INSPIRED BY MEGA CRIT" then "Created by Austin Somer" (from assets/pre-intro-images/*.png, sorted). tools/mkslides.py cover-crops each to 240x160, adaptive 256-color quantize → assets/slides/slides.bin (per slide: 256×u16 BGR555 palette + 240x160 byte indices, 38912 B/slide, 2 slides = 76KB) + src/slidesdata.s (.incbin) + src/slidesimg.h. slides_play() in intro.c (reuses the intro's mode-4 copy_page/load_pal): starts on black (BLDY=16), then per slide loads it under black → fade_in (16→0) → hold ~120 frames (2s) → fade_out (0→16); A/START skips a slide. New ungated fades in engine.c (fade_in/fade_out via bldy_ramp — distinct from the intro's pending-gated fade_to_black/fade_from_black) so each slide fades from black and to black independently; the fade-out-to-black then fade-in-from-black between the two IS the requested transition. Wired in main.c's cold-boot #else before intro_play() (bios → slides → video → title); test builds skip it. Makefile: slidesdata.o added. ROM 9248KB → 9344KB. VERIFIED in mGBA: both slides render crisp at full brightness in order (MEGA CRIT captured, then Austin Somer portrait), 60.7fps; AUTOPLAY ran the full bios→slides→video→title→Neow→combat chain (Green Louse fight, 60fps) with no crash/hang. Ship `make` (9344KB) boots. UPDATE (2026-07-14): a decorative 4-point star in the lower-right of both source PNGs was removed (delegated to a subagent — sampled the adjacent black bg and filled x[2200..) y[1360..) on both, star bbox was ~x2240-2335/y1400-1503 on a pure-black corner) and slides regenerated; verified the GBA-rendered 240x160 slide's lower-right corner max luminance is now 1-2 (black, star gone) with center content intact.

- [x] Intro audio (2026-07-14, tools/mkpcm.py + src/pcm.c + intro.c + game.h + pcmdata.s): the intro video now has music — "The Elite Slain" (~/Downloads, AV1+Opus mp4; only the audio is used). tools/mkpcm.py gained a `--intro <file>` mode: decodes/normalizes like the other tracks but as a ONE-SHOT (no loop crossfade; a ~0.08s tail fade so the hard stop doesn't click) → assets/music/intro.pcm (s8 mono 13379Hz, 284032 samples ≈ 21.2s, 277KB); it keeps the existing title/run .pcm and now writes pcmdata.s listing every track present on disk (title, run, intro). New PCM_INTRO (game.h). pcm.c: `cur_loop` flag — pcm_play sets it 0 for PCM_INTRO (1 for title/run); pcm_tick, on reaching cur_len, LOOPS if cur_loop else calls pcm_stop() (clean one-shot end: DMA1 off + timer0 off + FIFO A flush, so no underrun buzz). intro.c: pcm_play(PCM_INTRO) at the top of intro_play (starts with the video, plays over the video + the PRESS START hold — full length while waiting, per spec); on the start-press that exits the hold, pcm_stop() (user's simplified spec: no fade, just stop) before the fade_to_black. After intro_play returns, the main loop's pcm_play(PCM_TITLE) starts the title track (title_screen fades up). Music respects opt_music (settings). Slides remain silent (music starts at the video). No Makefile change (pcmdata.o already deps on assets/music/*.pcm). ROM 9344KB → 9600KB. VERIFIED: intro.pcm exported to WAV and sent to the user to judge by ear; AUTOPLAY ran the whole slides→intro(music)→title→run→combat→VICTORY chain at 60fps with no crash/hang/buzz (the pcm_play(INTRO)→pcm_stop→pcm_play(TITLE) path executed). Ship `make` (9600KB) boots. (Audio quality = user to judge from the WAV / on hardware.)

- Combat left-gutter EX/DN exhaust-pile label REMOVED (2026-07-13, user request — it clipped the energy orb/draw-pile icon and usually read "EX 0"). The exhaust pile still OPENS on DOWN in combat (draw_player, combat.c); only the on-screen label is gone. L=draw, UP=discard, DOWN=exhaust remain the pile-viewer keys.
